import Link from "next/link"; import { Container } from "@/components/container"; import { CodeBlock } from "@/components/code-block"; import { TokenGraphShowcase } from "./token-graph-showcase"; import { siteConfig as site } from "@/config/site";
export function Hero(){return <section id="product" className="hero"><Container><div className="hero-copy"><p className="eyebrow">Local token-flow analysis for LLM applications</p><h1>See exactly what consumes your LLM tokens.</h1><p>Trace prompts, context, variables and model calls through a local TokenGraph. Find expensive flows before changing production behavior.</p><div className="hero-actions"><Link className="button-primary" href={site.docs.quickstart}>Build your first TokenGraph</Link><a className="button-secondary" href={site.marketplaceUrl} target="_blank" rel="noopener noreferrer">Install the VS Code extension</a><a className="button-secondary" href={site.productRepositoryUrl} target="_blank" rel="noopener noreferrer">View on GitHub</a></div><CodeBlock code={site.installCommand}/><div className="truth-labels">{["Local-first","No mandatory API key","Python + JavaScript/TypeScript","MCP compatible","MIT licensed"].map(x=><span key={x}>{x}</span>)}</div></div><TokenGraphShowcase compact/></Container></section>}
